services:
  postgres:
    image: postgres:16
    container_name: pg
    restart: unless-stopped
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - ./data:${PGDATA}
      - ./init:/docker-entrypoint-initdb.d:ro
      - ./resources:/resources:ro
